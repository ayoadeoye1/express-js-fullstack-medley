<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User Details</title>
    <style>
        /* Global styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #FFFFFF; /* White background */
            color: #3E2723; /* Dark brown text */
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        h1 {
            text-align: center;
            color: #3E2723;
            margin-bottom: 20px;
        }

        form {
            background-color: #F5F5F5; /* Light grey background for form */
            padding: 20px;
            border-radius: 12px;
            max-width: 600px;
            width: 100%;
            box-shadow: 10px 10px 20px rgba(0, 0, 0, 0.1), -10px -10px 20px #FFFFFF;
        }

        label {
            color: #6D4C41;
            font-weight: bold;
            margin-top: 10px;
            display: block;
        }

        input[type="text"], input[type="date"], input[type="file"] {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 15px;
            border: none;
            border-radius: 8px;
            background-color: #E0E0E0;
            box-shadow: inset 4px 4px 8px rgba(0, 0, 0, 0.1), inset -4px -4px 8px #FFFFFF;
            font-size: 16px;
            color: #3E2723;
        }

        input[type="file"] {
            padding: 0;
        }

        button {
            background-color: #6D4C41;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            cursor: pointer;
            box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.1), -4px -4px 10px #FFFFFF;
            transition: 0.3s;
        }

        button:hover {
            box-shadow: inset 4px 4px 10px rgba(0, 0, 0, 0.1), inset -4px -4px 10px #FFFFFF;
        }

        a {
            display: block;
            text-align: center;
            color: #6D4C41;
            text-decoration: none;
            margin-top: 20px;
            padding: 10px;
            background-color: #E0E0E0;
            border-radius: 12px;
            box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.1), -4px -4px 10px #FFFFFF;
            transition: 0.3s;
        }

        a:hover {
            box-shadow: inset 4px 4px 10px rgba(0, 0, 0, 0.1), inset -4px -4px 10px #FFFFFF;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            form {
                padding: 15px;
            }

            input[type="text"], input[type="date"], input[type="file"] {
                font-size: 14px;
            }

            button {
                font-size: 14px;
                padding: 8px 16px;
            }

            a {
                font-size: 14px;
                padding: 8px;
            }
        }
    </style>
</head>
<body>
    <div>
        <h1>Edit Your Details</h1>
        <form action="/edit/<%= entry.Id %>" method="POST" enctype="multipart/form-data">
            <label for="Names">Names</label>
            <input type="text" name="Names" id="Names" value="<%= entry.Names %>" required />

            <label for="Contact">Contact</label>
            <input type="text" name="Contact" id="Contact" value="<%= entry.Contact %>" required />

            <label for="Image">Image</label>
            <input type="file" name="Image" id="Image" value="/files/<%= entry.Image %>" />

            <label for="DOB">DOB</label>
            <input type="date" name="DOB" id="DOB" value="<%= entry.DOB %>" required />

            <label for="Address">Address</label>
            <input type="text" name="Address" id="Address" value="<%= entry.Address %>" required />

            <label for="Favourite_Quote">Favourite Quote</label>
            <input type="text" name="Favourite_Quote" id="Favourite_Quote" value="<%= entry.Favourite_Quote %>" required />

            <button type="submit">Edit</button>
        </form>
        <div>
            <a href="/">Back To List</a>
        </div>
    </div>
</body>
</html>
